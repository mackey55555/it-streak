name: OTA Update (Production)

on:
  push:
    branches: [main]

jobs:
  ota_update:
    type: update
    params:
      branch: production
      platform: ios
      message: "OTA: ${{ github.event.head_commit.message }}"
